<%= @patient.name %>
<%= @patient.appointments.count %>
<%= @patient.appointments.each do |appointment| %>
<%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %>, Date/Time: <%= appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M")%>
<% end %>
